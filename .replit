run = "pip install -r backend/requirements.txt && uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
entrypoint = "backend/main.py"

[env]
PYTHONPATH = "."
PYTHONUNBUFFERED = "1"

[packager]
language = "python3"

[interpreter]
command = ["python3"]
